# iPedia HTML Tester v2

Render Static Site settings:

Build Command: echo "no build"
Publish Directory: .

This version supports:
- single .html upload
- .zip upload with index.html and relative assets
- iPhone Safari preview
- virtual preview via Service Worker + Cache API

Note: ZIP preview needs HTTPS. Render provides HTTPS automatically.
